[project]
name = "case_study"
version = "0.1.0"
description = "Cat/dog classifier used as a teaching example in the book"
requires-python = ">=3.10"
dependencies = [
  "albumentations>=1.4",
  "numpy",
  "Pillow",
  "scikit-learn",
  "torch",
]
